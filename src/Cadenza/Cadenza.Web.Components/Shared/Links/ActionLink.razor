<MudText Typo="@Typo">
    
    @if (Icon != null)
    {
        <MudIcon Icon="@Icon" Size="@Size.Small" Class="@IconClass" />
    }

    <MudLink Typo="@Typo" Style="cursor: pointer;" OnClick="@(() => OnClick())" Color="@Color.Surface" Class="@Class">
        @Text
    </MudLink>
</MudText>


@code {
    [Parameter] public Typo Typo { get; set; }
    [Parameter] public string Text { get; set; }
    [Parameter] public string Icon { get; set; }
    [Parameter] public string Class { get; set; }
    [Parameter] public Action OnClick { get; set; }

    private string IconClass => "mr-2 " + Class;
}
