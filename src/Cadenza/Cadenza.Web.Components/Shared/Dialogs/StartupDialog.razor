@using Cadenza.State.Store;

@inherits StartupDialogBase

<MudDialog>
    <DialogContent>
        @foreach (var connection in Connections)
        {
            <StartupProgressView Connector="@connection.Connector" />
        }
    </DialogContent>
</MudDialog>
