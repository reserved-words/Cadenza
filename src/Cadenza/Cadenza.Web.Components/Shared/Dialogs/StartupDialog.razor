@inherits StartupDialogBase

@if (Started)
{
    <MudDialog>
        <DialogContent>
            @foreach (var task in SubTasks.Values)
            {
                <StartupProgressView Task="@task" />
            }
        </DialogContent>
    </MudDialog>
}
