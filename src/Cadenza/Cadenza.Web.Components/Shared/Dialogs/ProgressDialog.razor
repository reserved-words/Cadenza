@inherits ProgressDialogBase

@if (Started)
{
    <MudDialog>
        <DialogContent>
            @foreach (var task in SubTasks.Values)
            {
                <TaskProgressView Task="@task" />
            }
        </DialogContent>
    </MudDialog>
}
