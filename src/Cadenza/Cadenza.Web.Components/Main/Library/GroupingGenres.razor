
<AppCardScrollContainer>
    <ChildContent>
        <AppTable T="@string"
                  Items="@Genres">
            <RowTemplate>
                <MudTd>
                    <GenreLink Typo="@Typo.body1" Genre="@context" Grouping="@Grouping" />
                </MudTd>
            </RowTemplate>
        </AppTable>
    </ChildContent>
</AppCardScrollContainer>

@code {
    [Parameter] public string Grouping { get; set; }
    [Parameter] public IReadOnlyCollection<string> Genres { get; set; }
}
