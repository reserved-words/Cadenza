@*@inherits SystemUpdateQueueBase*@

@*<AppTableScrollContainer>
    <AppDataTable Items="@Items"
                     Filter="@FilterFunc"
                     Title="File Updates Queue">
        <HeaderContent>
            <MudTh>Type</MudTh>
            <MudTh>Item</MudTh>
            <MudTh>Property</MudTh>
            <MudTh>Current</MudTh>
            <MudTh>Update</MudTh>
            <MudTh></MudTh>
        </HeaderContent>
        <RowTemplate>
            <MudTd>
                @context.ItemType
            </MudTd>
            <MudTd>
                @context.Item
            </MudTd>
            <MudTd>
                @context.Property
            </MudTd>
            <MudTd>
                @context.OriginalValue
            </MudTd>
            <MudTd>
                @context.UpdatedValue
            </MudTd>
            <MudTd>
                <MudIconButton OnClick="@(() => Remove(context))"
                               Variant="Variant.Outlined"
                               Color="@Color.Default"
                               Icon="@Icons.Material.Filled.Delete"
                               Size="@Size.Small"></MudIconButton>
            </MudTd>
        </RowTemplate>
    </AppDataTable>
</AppTableScrollContainer>*@
