
<ItemLink Typo="@Typo.h6" Type="@PlayerItemType.Track" Id="@Id" Name="@Title" />
<ItemLink Typo="@Typo.body1" Type="@PlayerItemType.Artist" Id="@ArtistId" Name="@ArtistName" />

@code
{
    [Parameter] public TrackDetailsVM Model { get; set; }

    public int ArtistId => Model?.ArtistId ?? 0;
    public string ArtistName => Model?.ArtistName ?? "Artist Name";
    public int Id => Model?.Id ?? 0;
    public string Title => Model?.Title ?? "Track Title";
}