
<TabGrid>

    <TabGridColumn md="3">
        <TabGridRow RowHeight="@RowHeight.Stretch">

            <MudCard Elevation="3">
                <MudCardHeader>
                    <MudText Typo="Typo.h6">Search Criteria</MudText>
                </MudCardHeader>
                <MudCardContent>
                    <MudTextField Label="Search"
                                  @bind-Value="SearchText"
                                  For="@(() => SearchText)" />
                    <MudSelect Label="Type"
                               @bind-Value="SearchType"
                               For="@(() => SearchType)"
                               T="@string">
                        @foreach (var type in ItemTypes.Keys)
                        {
                            <MudSelectItem Value="@type">@type</MudSelectItem>
                        }
                    </MudSelect>
                    <MudStack Row="true" Class="mt-3">
                        <MudButton Variant="@Variant.Outlined" OnClick="@OnSearch">Search</MudButton>
                        <MudButton Variant="@Variant.Outlined" OnClick="@OnClear">Clear</MudButton>
                    </MudStack>
                </MudCardContent>
            </MudCard>

        </TabGridRow>
    </TabGridColumn>

    <TabGridColumn md="9">
        <TabGridRow RowHeight="@RowHeight.Full">
            <AppCardScrollContainer>
                <HeaderContent>
                    <MudText Typo="Typo.h6">Search Results</MudText>
                </HeaderContent>
                <ChildContent>
                    <AppTable T="@SearchItemVM"
                              Items="@Results">
                        <RowTemplate>
                            <MudTd Class="td-icon">
                                <MudIcon Icon="@context.Type.GetIcon()" Size="@Size.Small" />
                            </MudTd>
                            <MudTd>
                                <ItemLink Typo="@Typo.body1" Type="@context.Type" Id="@context.Id" Name="@context.Name"></ItemLink>
                            </MudTd>
                            <MudTd>
                                <MudText Typo="Typo.body1">@context.Artist</MudText>
                            </MudTd>
                            <MudTd>
                                <MudText Typo="Typo.body1">@context.AlbumDisplay</MudText>
                            </MudTd>
                        </RowTemplate>
                    </AppTable>
                </ChildContent>
            </AppCardScrollContainer>
        </TabGridRow>
    </TabGridColumn>

</TabGrid>