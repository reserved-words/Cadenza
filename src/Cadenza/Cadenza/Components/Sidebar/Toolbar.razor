@inherits ToolbarBase

<MudDivider Class="my-0" />

<MudToolBar DisableGutters="true" Dense="true">
    <MudIconButton Icon="@Icons.Material.Filled.Refresh" OnClick="@OnClearSession" Edge="Edge.Start" Class="ml-1" />

    <MudSpacer />

    @foreach (var sourceStatus in ConnectorStatuses)
    {
        <ToolbarSource Model="@sourceStatus" />
    }

</MudToolBar>