@if (Artist != null)
{
    <AppCardDivided>
        <HeaderContent>
            <ArtistCardHeader Model="@Artist"
                              TitleOnly="false" />
        </HeaderContent>
        <ChildContent>
        </ChildContent>
    </AppCardDivided>
}

@code {
    [Parameter]
    public ArtistInfo Artist { get; set; } = new();
}
