@using Cadenza.Core.Common

@foreach (var model in Links)
{
    <WhipIconLink Model="@model" />
}

@code {
    [Parameter]
    public List<LinkViewModel> Links { get; set; } = new List<LinkViewModel>();
}