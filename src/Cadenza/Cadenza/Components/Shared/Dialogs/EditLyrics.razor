@inherits EditLyricsBase

<WhipDialog Cancel="OnCancel"
            CancelCaption="Cancel"
            Submit="OnSubmit"
            SubmitCaption="OK"
            Actions="@Actions">

    <ChildContent>
        <EditForm Model="@EditableItem">
            <DataAnnotationsValidator />

            <MudTextField Class="textarea"
                            @bind-Value="EditableItem.Lyrics"
                            For="@(() => EditableItem.Lyrics)"
                            Lines="30" />

@*            <MudCard Class="pa-0">
                <MudCardContent Class="pa-1">

                </MudCardContent>
                <MudCardActions>
                    <MudButton Variant="Variant.Text" Color="Color.Primary">Load</MudButton>
                    <MudButton Variant="Variant.Text" Color="Color.Primary" OnClick="@OnSearch">Search</MudButton>
                </MudCardActions>
            </MudCard>
*@        </EditForm>
    </ChildContent>

</WhipDialog>