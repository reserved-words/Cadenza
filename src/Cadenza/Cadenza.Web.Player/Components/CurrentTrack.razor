@using Cadenza.Common.Domain.Model.Track

<MudText Typo="@Typo.h6">@Title</MudText>
<MudText Typo="@Typo.body1">@Artist</MudText>

@*            <TrackLink Typo="@Typo.h6" Id="@Model.Track.Id" Title="@Model.Track.Title" />
    <ArtistLink Typo="@Typo.body1" Id="@Model.Artist.Id" Name="@Model.Artist.Name" />
*@

@code
{
    [Parameter]
    public TrackInfo Model { get; set; }

    public string Title => Model?.Title ?? "Track Title";
    public string Artist => Model?.ArtistName ?? "Artist Name";
    public string TrackId => Model?.Id;
    public string TrackArtistId => Model?.ArtistId;
}